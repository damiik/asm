(executable
    (name asm)
    (libraries asmparser preprocess tokenizer str)
    ; (preprocess (pps ppx_deriving.show))
    (public_name asm)    
     (package asmparser)
    )


;  (install
;     (package asmparser)
;     (section bin)
;     (files (asm.exe as asm)))   