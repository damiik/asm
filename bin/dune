(executable
    (name asm)
    (libraries parser tokenizer str)
    ; (preprocess (pps ppx_deriving.show))
    (public_name asm)    
     (package parser)
    )


;  (install
;     (package parser)
;     (section bin)
;     (files (asm.exe as asm)))   